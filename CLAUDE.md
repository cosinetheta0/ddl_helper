# 学习进度记录

## 会话日期
- 2026-03-09

## 今天学了什么
- 参考方向“关联/非关联”下的功率计算。
- 区分 `p吸` 与 `p发` 的符号与物理意义。
- 在电流为负值时，正确解释“约定方向”和“实际方向”的关系。

## 理解情况
- 理解较好：
  - 关联时可用 `p吸 = ui`，并能判断吸收/发出。
  - 非关联时能正确给出发出功率（如 `p发 = 20W`）并与 `p吸` 互相换算。
  - 能处理“关联但电流为负”“非关联且电流为负”的组合题。
- 仍需巩固：
  - 从“定义出发”快速口述完整三步法，避免先记结论再回推。

## 下次从哪里继续
- 先复盘并固化功率判断三步法（参考方向、功率定义、符号解释）。
- 再进入“含受控源/多元件电路中的功率守恒（总吸收=总发出）”的小题训练。

## 你的有价值见解
- “参考方向与实际情况无关。”
- “先看关联方向确定 `ui` 计算的是吸收还是发出，算出来如果是负数再更新下标得出结果。”
